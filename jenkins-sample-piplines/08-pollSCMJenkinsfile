pipeline {
    agent any
    options{
        timestamps()
    }
    triggers{
        pollSCM('* * * * *')
    }
    stages {
        stage("Cron Task") {
            steps {
                echo "Hey, There!"
                git url:"git@github.com:Mettilda-89/DEVOPS--TRAINING.git"
            }
        }
    }
}
